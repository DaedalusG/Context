import { IncomingMessage, ServerResponse } from "http";
export interface ContentSecurityPolicyOptions {
    directives?: {
        [directiveName: string]: Iterable<string>;
    };
    reportOnly?: boolean;
}
declare function contentSecurityPolicy(options?: Readonly<ContentSecurityPolicyOptions>): (_req: IncomingMessage, res: ServerResponse, next: () => void) => void;
export default contentSecurityPolicy;
